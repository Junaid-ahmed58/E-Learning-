class EnrollmentsController < ApplicationController
	# def create
	# 	@course = Course.find(params[course:id])
	# 	@enrollment = current_user_enrollment.build(course: @course)
	# 	if @enrollment.save
	# 		redirect_to course_path(@course), notice: 'You have successfully enrolled in the course.'
	# 	else
	# 		redirect_to course_path(@course), alert: 'There is an error for enrolling this course.'
	# 	end
	# end
end
